import { AppTheme } from "../types";
import { littleHouseTokens } from "../tokens/littleHouse.tokens";

export const littleHouseTheme: AppTheme = {
  id: "littleHouse",
  name: "Little House on the Prairie",
  colors: littleHouseTokens.colors,
  backgroundStyle: "littleHouse-checkerboard",
  fonts: littleHouseTokens.fonts,
  comingSoon: true,
};
