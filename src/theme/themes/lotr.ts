import { AppTheme } from "../types";
import { lotrTokens } from "../tokens/lotr.tokens";

export const lotrTheme: AppTheme = {
  id: "lotr",
  name: "Lord of the Rings",
  colors: lotrTokens.colors,
  fonts: lotrTokens.fonts,
};
